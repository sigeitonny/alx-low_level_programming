#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints last digit of the number
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	if n > 5
		printf("Last digit of %d" , 

